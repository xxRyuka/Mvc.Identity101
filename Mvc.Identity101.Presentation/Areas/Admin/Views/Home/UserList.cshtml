@model List<Mvc.Identity101.Areas.Admin.Data.Dto.UserListDto>

@{
    ViewBag.Title = "Kullanıcı Listesi";
    Layout = "_Layout";
}

<div class="admin-container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="admin-title mb-0">
            <i class="bi bi-people"></i>
            Kullanıcılar
        </h2>
        <div class="d-flex gap-2">
            <div class="input-group">
                <span class="input-group-text border-0">
                    <i class="bi bi-search"></i>
                </span>
                <input type="text" class="admin-form-control" id="userSearch" placeholder="Kullanıcı ara...">
            </div>
        </div>
    </div>

    <div class="table-responsive">
        <table class="admin-table" id="userTable">
            <thead>
                <tr>
                    <th>Kullanıcı Adı</th>
                    <th>E-posta</th>
                    <th>Telefon</th>
                    <th>Roller</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model)
                {
                    <tr>
                        <td>@user.UserName</td>
                        <td>@user.Email</td>
                        <td>@user.PhoneNumber</td>
                        <td>
                            @foreach (var role in user.Roles)
                            {
                                <span class="admin-badge admin-badge-blue me-1">@role</span>
                            }
                        </td>
                        <td>
                            <div class="admin-actions">
                                <a asp-controller="Role" asp-action="AssignRole" asp-route-id="@user.Id" 
                                   class="admin-icon-button" title="Rol Ata">
                                    <i class="bi bi-shield-plus"></i>
                                </a>
                                <button class="admin-icon-button" onclick="resetPassword('@user.Id')" title="Şifre Sıfırla">
                                    <i class="bi bi-key"></i>
                                </button>
                                <button class="admin-icon-button text-danger" onclick="deleteUser('@user.Id')" title="Kullanıcı Sil">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('userSearch').addEventListener('keyup', function() {
            var searchText = this.value.toLowerCase();
            var rows = document.querySelectorAll('#userTable tbody tr');
            
            rows.forEach(function(row) {
                var text = row.textContent.toLowerCase();
                row.style.display = text.includes(searchText) ? '' : 'none';
            });
        });

        function resetPassword(userId) {
            if (confirm('Kullanıcının şifresini sıfırlamak istediğinize emin misiniz?')) {
                // Şifre sıfırlama işlemi
            }
        }

        function deleteUser(userId) {
            if (confirm('Kullanıcıyı silmek istediğinize emin misiniz?')) {
                // Kullanıcı silme işlemi
            }
        }
    </script>
}
